// src/services/realtime/jobsBus.ts
// Note: This file is now simplified since the server doesn't support WebSockets.
// Job progress is handled individually by useJobProgress hook via polling.

export function ensureJobsBusStarted() {
  // No-op function to maintain compatibility with existing code
  // Individual job progress is now handled by useJobProgress hook
}
